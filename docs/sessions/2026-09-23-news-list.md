# Session: 2026-09-23 — news titles on launch

Mode: coach. Developer implemented and ran the tests; agent inspected source and documented the supplied output.
Milestone: M0, in progress.
Outcome: Android launch displays “Morning update” and “Science report”.
Out of scope: navigation, bookmarks, persistence, iOS verification, and CI implementation.

## Scenario and evidence

- [x] Launch the real MainActivity and display both bundled titles.
- [ ] Temporarily alter the second title, observe failure, restore it, and confirm green.
- [ ] Rerun host tests after the UI implementation.

Test boundary: Android instrumented Compose UI test launching MainActivity; actual shared App UI, fixed text, no repository double or network.

Command pasted by developer:

```sh
./gradlew :android:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=dev.toelie.hellokmp.NewsListTest
```

The failure output names `:androidApp:connectedDebugAndroidTest`; the canonical command is recorded in [local commands](../07-commands.md).

Observed red evidence supplied in conversation: one test executed on HMD ARC, Android 14; assertion failed because “Morning update” was not displayed; build failed in 16 seconds. This reached a behavioral assertion, rather than failing during setup. The first failure did not evaluate the second assertion.

Implementation inspected afterward: App renders both titles as Text elements in a Column within MaterialTheme. Both original title assertions remain in NewsListTest.

Observed green evidence supplied in conversation: one test started and finished on the same device; BUILD SUCCESSFUL in 17 seconds. The agent did not independently rerun the test.

Refactor: no structural change needed. Unused template imports remain as minor cleanup for the developer.

Wider evidence: baseline build/launch and host-test success were reported earlier. Post-feature host tests, deliberate-break check, CI, clean-checkout reproduction, and iOS checks have not been reported. M0 is not complete.

## Commit and learning

Git inspection returned “not a git repository” for the project root. No commit was created by the agent. Initialise and review the current snapshot before the first commit; do not claim an earlier template baseline commit exists.

The UI test proves these titles are visible through the Android launch path on the tested device. Arithmetic host tests alone could not prove that. Next: finish the local commit checkpoint, then address M0 CI evidence.
